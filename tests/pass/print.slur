; -*- scheme -*-
  

(module Main
  (define other ()
    ;run (print "asdf")
    return 4
    )

  (define prt(n)
    return (print n))
  
  (define main IO ()
    run (prt 3)
    run (print "hello")
    run (. putStr show ("asdf"))
    run (. putStr show (+ 0.345 3.234))
    temp = 345
    ;return (return "hello")
    ))